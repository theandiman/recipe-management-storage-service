name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

# Cancel in-progress builds for non-main branches (allow concurrent builds only on main)
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  ci:
    name: Build and Test
    uses: theandiman/recipe-management/.github/workflows/backend-java-ci.yml@0d0907639ec4a5c7cfed4ace1d574696c886591c
    permissions:
      contents: read
      actions: write
      packages: read
    with:
      version_strategy: version-script
      run_sonar: true
      sonar_project_key: theandiman_recipe-management-storage-service
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  cd:
    name: Deploy
    needs: ci
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    uses: theandiman/recipe-management/.github/workflows/backend-java-cloud-run-cd.yml@c9051e128e0cb76c0f057029629713d0ce1d0565
    permissions:
      contents: write
      actions: write
      id-token: write
    with:
      service_name: recipe-management-service
      artifact_registry_repository: recipe-management
      use_wif: true
    secrets:
      GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
      GCP_WORKLOAD_IDENTITY_PROVIDER: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
      GCP_DEPLOY_SA: ${{ secrets.GCP_DEPLOY_SA }}
