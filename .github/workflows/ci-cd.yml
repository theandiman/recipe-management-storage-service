name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

# Cancel in-progress builds for non-main branches (allow concurrent builds only on main)
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  ci:
    name: Build and Test
    uses: theandiman/recipe-management/.github/workflows/backend-java-ci.yml@8629e2a0e0d9b2d97c647dd51f60f3d08b399535
    permissions:
      contents: read
      actions: write
      packages: read
    with:
      version_strategy: version-script
      run_sonar: true
      sonar_project_key: theandiman_recipe-management-storage-service
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

  cd:
    name: Deploy
    needs: ci
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    uses: theandiman/recipe-management/.github/workflows/backend-java-cloud-run-cd.yml@8629e2a0e0d9b2d97c647dd51f60f3d08b399535
    permissions:
      contents: write
      actions: write
      id-token: write
    with:
      service_name: recipe-management-service
      artifact_registry_repository: recipe-management
      use_wif: true
      zap_rules: "10049\tIGNORE\t(Non-Storable Content - expected for auth-protected endpoints returning 401)"
    secrets:
      GCP_PROJECT_ID: ${{ secrets.GCP_PROJECT_ID }}
      GCP_WORKLOAD_IDENTITY_PROVIDER: ${{ secrets.GCP_WORKLOAD_IDENTITY_PROVIDER }}
      GCP_DEPLOY_SA: ${{ secrets.GCP_DEPLOY_SA }}
      GIT_PAT: ${{ secrets.GIT_PAT }}
